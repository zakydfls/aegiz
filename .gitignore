.DS_Store
.build/
.swiftpm/
DerivedData/
target/
dist/
*.xcuserstate
*.xcuserdatad/
*.xcworkspace/xcuserdata/
*.moved-aside
*.swp
*.swo
*.tmp
*.temp
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal
.aegiz/
apps/macos/Frameworks/GhosttyKit.xcframework/

# Local environment and credential material. Commit a deliberately redacted
# `*.example` or `*.template` file when documentation needs one.
.env
.env.*
!.env.example
!.env.template
*.env.local
*.local.env
/credentials/
/secrets/
/private/
/vault-backups/

# Private keys, certificates, signing profiles, and password vaults.
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
*.der
*.mobileprovision
*.provisionprofile
*.jks
*.keystore
*.kdbx
*.age
*.agekey
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_ecdsa.pub
authorized_keys
known_hosts

# Local exports, backups, diagnostics, and application state can contain
# connection profiles, host metadata, or command output.
*.backup
*.bak
*.dump
*.sql.gz
*.log
*.crash
*.dmp
*.stackdump

# Local planning, AI checkpoints, and internal documentation.
# Keep only the public root README in version control.
*.md
!/README.md
/docs/
